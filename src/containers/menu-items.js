import React, { Component } from 'react';

export default MenuItems extends Component {
  render(){
    return (
      <div>
        <ul className="menu-items">
        //a list of menu items available to order
        </ul>
      </div>
    );
  }
}
